{
  "name": "js-capacity-test",
  "version": "0.0.1",
  "description": "JS capacity test",
  "main": "todo-node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "connect": "~2.10.1"
  }
}
